export type Model = { name: string, path: string, origin: string };

const models: Model[] = [
    {
        name: 'Human Skull',
        path: './static/models/human-skull.obj',
        origin: 'https://sketchfab.com/martinjario'
    }
];

export default models;
